---
import { COMMENTS } from "@/consts";

// Renders nothing until the owner enables giscus in consts.ts.
const ready =
  COMMENTS.ENABLED && COMMENTS.REPO_ID !== "" && COMMENTS.CATEGORY_ID !== "";
---

{
  ready && (
    <section class="mx-auto max-w-6xl px-5 pb-12">
      <div class="mx-auto max-w-4xl">
        <p class="mono text-accent-text mb-4 text-xs font-bold tracking-widest uppercase">
          ▸ Comments
        </p>
        <div
          id="giscus-mount"
          class="border-ink bg-paper shadow-neo-sm rounded-[4px] border-[3px] p-4"
          data-giscus-repo={COMMENTS.REPO}
          data-giscus-repo-id={COMMENTS.REPO_ID}
          data-giscus-category={COMMENTS.CATEGORY}
          data-giscus-category-id={COMMENTS.CATEGORY_ID}
          data-giscus-mapping={COMMENTS.MAPPING}
          data-giscus-reactions={COMMENTS.REACTIONS ? "1" : "0"}
        />
      </div>
    </section>
  )
}

{ready && <script>import "@/scripts/giscus";</script>}

<style>
  /* giscus's client.js injects <link href="https://giscus.app/default.css">,
     which our CSP (style-src 'self' 'unsafe-inline') blocks — leaving the
     iframe at its default 300px intrinsic width. Ship those rules ourselves
     instead of widening the CSP. The loading fade is set inline by client.js. */
  #giscus-mount :global(.giscus),
  #giscus-mount :global(.giscus-frame) {
    width: 100%;
  }

  #giscus-mount :global(.giscus-frame) {
    border: none;
    color-scheme: normal;
  }
</style>
