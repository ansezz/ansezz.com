---
type Tone = "yellow" | "pink" | "cyan" | "green";

interface Props {
  tone?: Tone;
  tilt?: number;
  class?: string;
}

const { tone = "yellow", tilt = -2, class: className = "" } = Astro.props;

const toneClass: Record<Tone, string> = {
  yellow: "bg-yellow",
  pink: "bg-pink",
  cyan: "bg-cyan",
  green: "bg-green",
};

const style = `transform: rotate(${tilt}deg);`;
---

<div
  class:list={[
    "border-ink shadow-neo-md inline-block max-w-xs border-[3px] p-4",
    toneClass[tone],
    className,
  ]}
  style={style}
>
  <slot />
</div>
