---
type Tone = "yellow" | "pink" | "cyan" | "green";

interface Props {
  tone?: Tone;
  tilt?: number;
  class?: string;
}

const { tone = "yellow", tilt = -2, class: className = "" } = Astro.props;

const toneClass: Record<Tone, string> = {
  yellow: "bg-yellow text-on-accent",
  pink: "bg-pink text-on-accent",
  cyan: "bg-cyan text-on-accent",
  green: "bg-green text-on-accent",
};

const style = `transform: rotate(${tilt}deg);`;
---

<div
  class:list={[
    "border-ink shadow-neo-md inline-block w-full max-w-xs border-[3px] p-4",
    toneClass[tone],
    className,
  ]}
  style={style}
>
  <slot />
</div>
