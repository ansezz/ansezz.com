---
type Tone = "paper" | "yellow" | "pink" | "cyan" | "green";
type Tail = "bl" | "br" | "tl" | "tr";

interface Props {
  tone?: Tone;
  tail?: Tail;
  class?: string;
}

const { tone = "paper", tail = "bl", class: className = "" } = Astro.props;

const toneClass: Record<Tone, string> = {
  paper: "bg-paper",
  yellow: "bg-yellow",
  pink: "bg-pink",
  cyan: "bg-cyan",
  green: "bg-green",
};

const toneFill: Record<Tone, string> = {
  paper: "var(--color-paper)",
  yellow: "var(--color-yellow)",
  pink: "var(--color-pink)",
  cyan: "var(--color-cyan)",
  green: "var(--color-green)",
};

const tailPos: Record<Tail, string> = {
  bl: "left-8 -bottom-5",
  br: "right-8 -bottom-5",
  tl: "left-8 -top-5 rotate-180",
  tr: "right-8 -top-5 rotate-180",
};
---

<div
  class:list={[
    "border-ink shadow-neo-md text-ink relative inline-block rounded-[6px] border-[3px] px-5 py-4 font-bold",
    toneClass[tone],
    className,
  ]}
>
  <slot />
  <svg
    class:list={["absolute h-6 w-8", tailPos[tail]]}
    viewBox="0 0 32 24"
    aria-hidden="true"
  >
    <path d="M0 0 L32 0 L8 24 Z" fill={toneFill[tone]}></path>
    <path
      d="M0 0 L32 0 L8 24 Z"
      fill="none"
      stroke="var(--color-ink)"
      stroke-width="3"
      stroke-linejoin="round"></path>
  </svg>
</div>
