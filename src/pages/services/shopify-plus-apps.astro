---
import ServiceLanderPage from "@/components/services/ServiceLanderPage.astro";
import { SERVICE_LANDERS_BY_SLUG } from "@/consts";

const lander = SERVICE_LANDERS_BY_SLUG["shopify-plus-apps"];
if (!lander) throw new Error("Missing service lander: shopify-plus-apps");
---

<ServiceLanderPage lander={lander} />
